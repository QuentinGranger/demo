name: Patch Pokémon GO Hordes Méga 2026-09-02

on:
  push:
    branches: [master]
    paths:
      - 'scripts/patch_pokemon_go_mega_squads_20260902.py'
      - '.github/workflows/patch-pokemon-go-mega-squads-20260902.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Patch calendar
        run: python scripts/patch_pokemon_go_mega_squads_20260902.py
      - name: Validate calendar integrity
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          s=Path('calendars/pokemon-paris.ics').read_text(encoding='utf-8')
          assert s.count('BEGIN:VCALENDAR') == 1
          assert s.count('END:VCALENDAR') == 1
          uids=re.findall(r'^UID:(.+?)\r?$', s, flags=re.M)
          assert len(uids) == len(set(uids))
          assert 'UID:pokemon-go-hordes-mega-20260908@openai' in s
          assert s.rstrip().endswith('END:VCALENDAR')
          PY
      - name: Commit calendar if changed
        run: |
          if git diff --quiet -- calendars/pokemon-paris.ics; then
            echo 'No calendar change'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add calendars/pokemon-paris.ics
          git commit -m 'calendar: add Pokémon GO Hordes Méga'
          git push origin HEAD:master
